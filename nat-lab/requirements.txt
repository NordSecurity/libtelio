aiodocker ~= 0.21
mypy ~= 0.910
pytest ~= 7.2
pytest-asyncio ~= 0.18
asyncssh ~= 2.9
pytest-rerunfailures ~= 10.2
dataclasses-json ~= 0.5.7
pytest-order ~= 1.0.1
pytest-timeout == 2.1.0
flatten_json ~= 0.1.13

# Latest version compatible with protobuf 3.20.3
types-protobuf == 3.19.21
# Must match libprotoc version used to generate proto files
protobuf == 3.20.3

# Fixes 'asyncssh' cryptography deprecation warnings. If there aren't anymore warnings
# after running tests, this dependency can be removed.
# https://github.com/iterative/dvc/issues/7656
cryptography ~= 36.0
